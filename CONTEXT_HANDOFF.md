# CONTEXT_HANDOFF

## 当前目标
- 修复 6.11 三类稳定性问题：大屏第二批消息多选到底后找不到底部“选择到这里”；第二批只选中最后一条漏选上方消息；小屏选群计数不可读导致假确认、最终数量不足。

## 已完成
- `MessageForwarder` 全选流程增加到底恢复：到底后只看到上半屏“选择到这里”或无底部按钮时，有限次小幅微调，只点击下半屏按钮。
- 全选失败时退出多选并重新定位锚点重试 1 次，仍失败才 dump 终止。
- 点击“选择到这里”后读取多选页 `转发(N)` 数量；若 `N < 未读触发数`，判定疑似只选中最后一条，退出多选重试。
- 兼容 `转发(N)` 按钮文本/描述，避免数量校验后下一步找不到“转发”。
- 小屏选群点击收紧：复选框 bounds 必须完整处于安全区内，不再只看中心点。
- 计数不可读时不再默认成功；只有复选框明确 checked/selected 才移入待最终确认，否则保留待选继续扫描/重试。
- 点击前若复选框已处于选中状态，直接按已选处理，避免再次点击取消。

## 已修改文件
- `android/WeworkForwarder/app/src/main/java/com/wework/forwarder/MessageForwarder.kt`
- `CONTEXT_HANDOFF.md`

## 关键决策
- 仍然不允许部分发送；最终 `确定(N)` 必须等于本批目标数。
- 上半屏唯一“选择到这里”仍不点击，避免选到旧消息。
- `转发(N)` 数量读不到时不阻断，保持兼容；只有读到且数量不足才拒绝。
- 不修改选群搜索/蓝勾图像识别，不改目标群配置。

## 验证情况
- `git diff --check` 通过。
- `JAVA_HOME=/opt/homebrew/opt/openjdk@17 ANDROID_HOME=/opt/homebrew/share/android-commandlinetools ./gradlew assembleDebug` 通过。

## 未完成事项
- 待真机验证：大屏第 2 批全选恢复且 `转发(N)` 不小于未读数；小屏花月仙鲜花4/8/9 不再假确认，最终必须 `确定(9)` 才发送。
- 待按需提交、打 tag、触发 CI。

## 已知问题
- 未跟踪文件 `1.jpg` 不属于本次改动，不应提交。
